@using Pb.Wechat.Web.Resources
@using Pb.Wechat.Web.Views.Shared.Components.TenantChange
@using Pb.Wechat
@model TenantChangeViewModel
@inject IWebResourceManager WebResourceManager
@{
    WebResourceManager.AddScript(AppConsts.MvcAppPath + "view-resources/Views/Shared/Components/TenantChange/Default.js");
}
<span class="tenant-change-component">
    @L("CurrentTenant"):

    @if (Model.Tenant != null)
    {
        <span title="@Model.Tenant.Name"><strong>@Model.Tenant.TenancyName</strong></span>
    }
    else
    {
        <span>@L("NotSelected")</span>
    }

    (<a href="#">@L("Change")</a>)
</span>